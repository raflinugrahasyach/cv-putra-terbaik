import ProjectsClient from './ProjectsClient';

export const metadata = {
  title: 'Galeri Proyek & Portfolio | CV Putra Terbaik',
  description: 'Lihat dokumentasi pemasangan barrier gate, sistem parkir manless, dan tripod turnstile di RSUD, Perumahan, dan Instansi Pemerintah.',
};

export default function ProjectsPage() {
  return <ProjectsClient />;
}